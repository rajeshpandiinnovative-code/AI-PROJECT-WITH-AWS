/** Printed after `npm run qa` succeeds. */
console.log(`
── Next on your machine ──
  npm run dev                    → http://localhost:3000
  npm run seed:verify-school     → optional demo School ID for /login
  npm run verify                 → env + lint + tsc (needs .env.local)
  npm run build                  → production compile check

See README: onboarding, UDISE import:schools, register/login.
`);
