# Dependencies (never commit)
node_modules/

# Secrets & local env (Vercel / GitHub Actions inject vars in the dashboard; never commit AWS, Gemini, or DB URLs)
.env
.env.local
.env*.local

/src/data/db.json
.DS_Store
npm-debug.log*
test-results/
playwright-report/

# Next.js / local
.next/
out/
*.tsbuildinfo
.workspace-backups/
